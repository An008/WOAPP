// Iron Protocol - app.js
// Bootstrap - loaded last
// ASCII-ONLY: no byte above 0x7F may appear in this file.

window.onload=function(){
  var el=document.getElementById('ls-name');
  if(el)el.focus();
};
